import { styled } from '@mui/system';

export const StyledDateWrapper = styled('div')({
  display: 'flex',
  justifyContent: 'center',
});

// export const LocalizationProvider = styled('div')({
//   margin: '15px',
// });
